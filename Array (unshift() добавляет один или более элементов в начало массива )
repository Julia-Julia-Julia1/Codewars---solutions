
function arrayOfDigits(n) {
    const arr = [];
    do {
        arr.unshift(n % 10);
        n = (n - n % 10) / 10
    }
    while (n);
    return arr;
}
// функция arrayOfDigits(123456) должна возвратить [1, 2, 3, 4, 5, 6];



